import React from 'react';
import Servicios from './Servicios';

/**
 * Componente de redirección para mantener compatibilidad con las rutas
 * Este componente simplemente renderiza el componente Servicios
 */
const Services = () => {
  return <Servicios />;
};

export default Services;
